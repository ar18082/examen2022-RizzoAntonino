{% extends 'base.html.twig' %}
{% form_theme form 'bootstrap_4_layout.html.twig' %}

{% block title %}Modifier le genre{% endblock %}

{% block body %}
<div class="container">
    <h1>{{ controller_name }}! </h1>

</div>
<div class="container">
    {{form_start(form)}}
    {{form_widget(form)}}
    {{form_end(form)}}

</div>

{% endblock %}